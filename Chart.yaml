---
apiVersion: v2
description: >-
  Helm chart for cluster-wide Vault Secrets Store CSI support on OpenShift. It focuses on
  provider-facing trust material (optional synced CA ConfigMap) across hub, spoke, and external
  Vault topologies. App-specific SecretProviderClass manifests are split into a dedicated chart.
keywords:
  - openshift
  - secrets-store-csi-driver
  - vault
name: openshift-sscsi-vault
version: 0.0.19
