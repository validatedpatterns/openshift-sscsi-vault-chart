---
apiVersion: v2
description: >-
  Helm chart (library-style) for Vault Secrets Store CSI on OpenShift: hub Vault URL,
  Kubernetes auth mount/role parity with openshift-external-secrets-chart patterns, and workload RBAC.
  Consuming charts should render named templates via include (see README); optional bundled manifests
  are off by default.
keywords:
  - openshift
  - secrets-store-csi-driver
  - vault
name: openshift-sscsi-vault
version: 0.0.15
