suite: SecretProviderClass Vault hub/spoke parity
templates:
  - templates/csi-library.tpl
  - templates/install-default-manifests.yaml
release:
  name: release-test
tests:
  - it: renders nothing when SecretProviderClass is disabled
    template: templates/install-default-manifests.yaml
    set:
      ocpSecretsStoreCsiVault:
        secretProviderClass:
          installDefaultManifests: true
          enabled: false
    asserts:
      - hasDocuments:
          count: 0

  - it: sets vaultAddress to hub Vault route pattern
    template: templates/install-default-manifests.yaml
    set:
      ocpSecretsStoreCsiVault:
        secretProviderClass:
          installDefaultManifests: true
      global:
        hubClusterDomain: apps.hub.example.org
    documentSelector:
      path: kind
      value: SecretProviderClass
    asserts:
      - equal:
          path: spec.parameters.vaultAddress
          value: "https://vault-vault.apps.hub.example.org"

  - it: uses external vault address when externalAddress is set
    template: templates/install-default-manifests.yaml
    set:
      ocpSecretsStoreCsiVault:
        secretProviderClass:
          installDefaultManifests: true
        vault:
          externalAddress: "https://vault.external.example:8200"
      global:
        hubClusterDomain: apps.hub.example.org
    documentSelector:
      path: kind
      value: SecretProviderClass
    asserts:
      - equal:
          path: spec.parameters.vaultAddress
          value: "https://vault.external.example:8200"

  - it: uses hub mount and hub role on hub cluster
    template: templates/install-default-manifests.yaml
    set:
      clusterGroup:
        isHubCluster: true
      ocpSecretsStoreCsiVault:
        secretProviderClass:
          installDefaultManifests: true
        vault:
          hubMountPath: hub
        rbac:
          rolename: hub-role
    documentSelector:
      path: kind
      value: SecretProviderClass
    asserts:
      - equal:
          path: spec.parameters.vaultKubernetesMountPath
          value: hub
      - equal:
          path: spec.parameters.roleName
          value: hub-role

  - it: uses spoke mount and spoke role on spoke cluster
    template: templates/install-default-manifests.yaml
    set:
      ocpSecretsStoreCsiVault:
        secretProviderClass:
          installDefaultManifests: true
      clusterGroup:
        isHubCluster: false
      global:
        clusterDomain: spoke.example.com
    documentSelector:
      path: kind
      value: SecretProviderClass
    asserts:
      - equal:
          path: spec.parameters.vaultKubernetesMountPath
          value: spoke.example.com
      - equal:
          path: spec.parameters.roleName
          value: spoke.example.com-role

  - it: uses hub auth when hashicorp-vault application is present on spoke
    template: templates/install-default-manifests.yaml
    set:
      ocpSecretsStoreCsiVault:
        secretProviderClass:
          installDefaultManifests: true
      clusterGroup:
        isHubCluster: false
        applications:
          vault:
            chart: hashicorp-vault
    documentSelector:
      path: kind
      value: SecretProviderClass
    asserts:
      - equal:
          path: spec.parameters.vaultKubernetesMountPath
          value: hub
      - equal:
          path: spec.parameters.roleName
          value: hub-role

  - it: normalizes vaultSkipTLSVerify to true for YAML boolean true
    template: templates/install-default-manifests.yaml
    set:
      ocpSecretsStoreCsiVault:
        secretProviderClass:
          installDefaultManifests: true
        tls:
          vaultSkipTLSVerify: true
      global:
        hubClusterDomain: apps.hub.example.org
    documentSelector:
      path: kind
      value: SecretProviderClass
    asserts:
      - equal:
          path: spec.parameters.vaultSkipTLSVerify
          value: "true"
      - notExists:
          path: spec.parameters.vaultCACertPath

  - it: normalizes empty vaultSkipTLSVerify to false for vault-csi-provider ParseBool
    template: templates/install-default-manifests.yaml
    set:
      ocpSecretsStoreCsiVault:
        secretProviderClass:
          installDefaultManifests: true
        tls:
          vaultSkipTLSVerify: ""
      global:
        hubClusterDomain: apps.hub.example.org
    documentSelector:
      path: kind
      value: SecretProviderClass
    asserts:
      - equal:
          path: spec.parameters.vaultSkipTLSVerify
          value: "false"

  - it: passes vaultCACertPath when set
    template: templates/install-default-manifests.yaml
    set:
      ocpSecretsStoreCsiVault:
        secretProviderClass:
          installDefaultManifests: true
        tls:
          vaultCACertPath: /etc/vault-ca/ca.crt
    documentSelector:
      path: kind
      value: SecretProviderClass
    asserts:
      - equal:
          path: spec.parameters.vaultCACertPath
          value: /etc/vault-ca/ca.crt

  - it: omits vaultCACertPath when empty and caProvider is disabled
    template: templates/install-default-manifests.yaml
    set:
      ocpSecretsStoreCsiVault:
        secretProviderClass:
          installDefaultManifests: true
        caProvider:
          enabled: false
        tls:
          vaultCACertPath: ""
    documentSelector:
      path: kind
      value: SecretProviderClass
    asserts:
      - notExists:
          path: spec.parameters.vaultCACertPath

  - it: defaults to CNO injected trust path when injectTrustedCabundle is null (key cleared by merge)
    template: templates/install-default-manifests.yaml
    set:
      clusterGroup:
        isHubCluster: true
      ocpSecretsStoreCsiVault:
        secretProviderClass:
          installDefaultManifests: true
        tls:
          vaultCACertPath: ""
        caProvider:
          syncProviderCaConfigMap:
            injectTrustedCabundle: null
    documentSelector:
      path: kind
      value: SecretProviderClass
    asserts:
      - equal:
          path: spec.parameters.vaultCACertPath
          value: /etc/pki/vault-ca/ca-bundle.crt

  - it: defaults vaultCACertPath on hub when tls path empty and caProvider enabled (CNO injected trust path)
    template: templates/install-default-manifests.yaml
    set:
      clusterGroup:
        isHubCluster: true
      ocpSecretsStoreCsiVault:
        secretProviderClass:
          installDefaultManifests: true
        tls:
          vaultCACertPath: ""
    documentSelector:
      path: kind
      value: SecretProviderClass
    asserts:
      - equal:
          path: spec.parameters.vaultCACertPath
          value: /etc/pki/vault-ca/ca-bundle.crt

  - it: defaults vaultCACertPath on spoke when tls path empty and caProvider enabled (CNO injected trust path)
    template: templates/install-default-manifests.yaml
    set:
      clusterGroup:
        isHubCluster: false
      ocpSecretsStoreCsiVault:
        secretProviderClass:
          installDefaultManifests: true
        tls:
          vaultCACertPath: ""
    documentSelector:
      path: kind
      value: SecretProviderClass
    asserts:
      - equal:
          path: spec.parameters.vaultCACertPath
          value: /etc/pki/vault-ca/ca-bundle.crt

  - it: uses same CNO trust path on spoke when hashicorp-vault app is present
    template: templates/install-default-manifests.yaml
    set:
      clusterGroup:
        isHubCluster: false
        applications:
          vault:
            chart: hashicorp-vault
      ocpSecretsStoreCsiVault:
        secretProviderClass:
          installDefaultManifests: true
        tls:
          vaultCACertPath: ""
    documentSelector:
      path: kind
      value: SecretProviderClass
    asserts:
      - equal:
          path: spec.parameters.vaultCACertPath
          value: /etc/pki/vault-ca/ca-bundle.crt

  - it: legacy Ansible CM path when injectTrustedCabundle and createConfigMap are false
    template: templates/install-default-manifests.yaml
    set:
      clusterGroup:
        isHubCluster: true
      ocpSecretsStoreCsiVault:
        secretProviderClass:
          installDefaultManifests: true
        tls:
          vaultCACertPath: ""
        caProvider:
          syncProviderCaConfigMap:
            injectTrustedCabundle: false
            createConfigMap: false
    documentSelector:
      path: kind
      value: SecretProviderClass
    asserts:
      - equal:
          path: spec.parameters.vaultCACertPath
          value: /etc/pki/vault-ca/vault-tls-ca.pem

  - it: falls back to kube-root default on hub when syncProviderCaConfigMap is disabled
    template: templates/install-default-manifests.yaml
    set:
      clusterGroup:
        isHubCluster: true
      ocpSecretsStoreCsiVault:
        secretProviderClass:
          installDefaultManifests: true
        tls:
          vaultCACertPath: ""
        caProvider:
          syncProviderCaConfigMap:
            enabled: false
    documentSelector:
      path: kind
      value: SecretProviderClass
    asserts:
      - equal:
          path: spec.parameters.vaultCACertPath
          value: /etc/pki/vault-ca/kube-root-ca.crt

  - it: falls back to hub-kube-root path on spoke when syncProviderCaConfigMap is disabled
    template: templates/install-default-manifests.yaml
    set:
      clusterGroup:
        isHubCluster: false
      ocpSecretsStoreCsiVault:
        secretProviderClass:
          installDefaultManifests: true
        tls:
          vaultCACertPath: ""
        caProvider:
          syncProviderCaConfigMap:
            enabled: false
    documentSelector:
      path: kind
      value: SecretProviderClass
    asserts:
      - equal:
          path: spec.parameters.vaultCACertPath
          value: /etc/pki/vault-ca/hub-kube-root-ca.crt

  - it: explicit vaultCACertPath overrides caProvider defaults
    template: templates/install-default-manifests.yaml
    set:
      clusterGroup:
        isHubCluster: true
      ocpSecretsStoreCsiVault:
        secretProviderClass:
          installDefaultManifests: true
        tls:
          vaultCACertPath: /etc/vault-ca/explicit.crt
    documentSelector:
      path: kind
      value: SecretProviderClass
    asserts:
      - equal:
          path: spec.parameters.vaultCACertPath
          value: /etc/vault-ca/explicit.crt

  - it: injectTrustedCabundle renders ConfigMap with CNO injection label and empty data
    template: templates/install-default-manifests.yaml
    set:
      clusterGroup:
        isHubCluster: true
      ocpSecretsStoreCsiVault:
        secretProviderClass:
          installDefaultManifests: true
        tls:
          vaultCACertPath: ""
        caProvider:
          syncProviderCaConfigMap:
            createConfigMap: true
            injectTrustedCabundle: true
    documentSelector:
      path: kind
      value: ConfigMap
    asserts:
      - equal:
          path: metadata.labels["config.openshift.io/inject-trusted-cabundle"]
          value: "true"
      - equal:
          path: metadata.annotations["argocd.argoproj.io/ignore-differences"]
          value: /data/ca-bundle.crt
      - isEmpty:
          path: data

  - it: inject ConfigMap Argo ignore pointer tracks trustedCabundleDataKey
    template: templates/install-default-manifests.yaml
    set:
      clusterGroup:
        isHubCluster: true
      ocpSecretsStoreCsiVault:
        secretProviderClass:
          installDefaultManifests: true
        tls:
          vaultCACertPath: ""
        caProvider:
          syncProviderCaConfigMap:
            createConfigMap: true
            injectTrustedCabundle: true
            trustedCabundleDataKey: custom-bundle.pem
    documentSelector:
      path: kind
      value: ConfigMap
    asserts:
      - equal:
          path: metadata.annotations["argocd.argoproj.io/ignore-differences"]
          value: /data/custom-bundle.pem

  - it: inject ConfigMap omits Argo ignore annotation when argocdIgnoreInjectedTrustedCabundleData is false
    template: templates/install-default-manifests.yaml
    set:
      clusterGroup:
        isHubCluster: true
      ocpSecretsStoreCsiVault:
        secretProviderClass:
          installDefaultManifests: true
        tls:
          vaultCACertPath: ""
        caProvider:
          syncProviderCaConfigMap:
            createConfigMap: true
            injectTrustedCabundle: true
            argocdIgnoreInjectedTrustedCabundleData: false
    documentSelector:
      path: kind
      value: ConfigMap
    asserts:
      - notExists:
          path: metadata.annotations["argocd.argoproj.io/ignore-differences"]

  - it: injectTrustedCabundle sets vaultCACertPath to trustedCabundleDataKey under mountDir
    template: templates/install-default-manifests.yaml
    set:
      clusterGroup:
        isHubCluster: true
      ocpSecretsStoreCsiVault:
        secretProviderClass:
          installDefaultManifests: true
        tls:
          vaultCACertPath: ""
        caProvider:
          syncProviderCaConfigMap:
            createConfigMap: true
            injectTrustedCabundle: true
            trustedCabundleDataKey: ca-bundle.crt
    documentSelector:
      path: kind
      value: SecretProviderClass
    asserts:
      - equal:
          path: spec.parameters.vaultCACertPath
          value: /etc/pki/vault-ca/ca-bundle.crt

  - it: injectTrustedCabundle with createConfigMap false still sets vaultCACertPath for out-of-band CM
    template: templates/install-default-manifests.yaml
    set:
      clusterGroup:
        isHubCluster: true
      ocpSecretsStoreCsiVault:
        secretProviderClass:
          installDefaultManifests: true
        tls:
          vaultCACertPath: ""
        caProvider:
          syncProviderCaConfigMap:
            createConfigMap: false
            injectTrustedCabundle: true
    documentSelector:
      path: kind
      value: SecretProviderClass
    asserts:
      - equal:
          path: spec.parameters.vaultCACertPath
          value: /etc/pki/vault-ca/ca-bundle.crt

  - it: renders secretObjects when provided
    template: templates/install-default-manifests.yaml
    set:
      ocpSecretsStoreCsiVault:
        secretProviderClass:
          installDefaultManifests: true
        secretObjects:
          - secretName: synced
            type: Opaque
            data:
              - objectName: example-password
                key: password
    documentSelector:
      path: kind
      value: SecretProviderClass
    asserts:
      - equal:
          path: spec.secretObjects[0].secretName
          value: synced
